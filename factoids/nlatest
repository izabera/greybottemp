Print N latest files, requires GNU find + sort: nlatest() { find "${2:-.}" -maxdepth 1 -type f -printf '%T+/%p\0' | sort -zr | for ((i=0; i<$1; i++)); do IFS=/ read -rd '' _ path || break; printf '%s\n' "$path"; done; } # Usage: nlatest N [directory]